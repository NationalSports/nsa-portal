{
  "name": "nsa-bot-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Always-on Playwright worker that runs Claude Code behind the 'Chief of Staff (Grok Bot)' portal identity and fills vendor carts for human review.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node worker.js",
    "once": "RUN_ONCE=1 node worker.js",
    "test:harness": "node test/spec.mjs",
    "test:agent": "node test/run-agent.mjs",
    "mock-portal": "node test/mock-portal.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
